const sum = (num1: number, num2: number): number => {
  return num1 + num2;
};

const result = sum(1, 2);
//  result. // api를 자동완성 하여 오타없이 사용할 수 있음.
// sum(1, "2"); // Error : string is not assignable
